# DANH SÁCH CÔNG VIỆC CẦN LÀM (TODO LIST) - 01/06/2026
*Dự án: FOOD AI*
*Nhánh phát triển hiện tại: feature/admin-visual-grid*

Bản kế hoạch hành động chi tiết này được tổng hợp từ các báo cáo kiểm toán codebase ngày 29/05/2026 nhằm khắc phục triệt để các vấn đề chất lượng giao diện, kiểu dữ liệu TypeScript, và các vi phạm kiến trúc hệ thống.

---

## 📌 PHẦN 1: NHÁNH "ADMIN-VISUAL-GRID" (GIAO DIỆN & KIỂU DỮ LIỆU)

Các nhiệm vụ tối ưu hóa chất lượng giao diện lưới trực quan và giải quyết dứt điểm các cảnh báo kiểu dữ liệu khi khôi phục code từ stash:

### 🔴 Mức độ: CAO (High Priority)
- [x] **Sửa lỗi CSS Tailwind trong `MiniCardForAdmin.tsx` & `MiniFoodCard.tsx`:**
  - *Vị trí:* `my-web/frontend/src/components/features/admin/MiniCardForAdmin.tsx` và `my-web/frontend/src/components/features/food/MiniFoodCard.tsx`
  - *Vấn đề:* Đang sử dụng class không tồn tại trong Tailwind mặc định là `bg-gray-55` và `border-slate-850`.
  - *Giải pháp:* Thay thế bằng `bg-gray-50` và `border-slate-800` để hiển thị đúng thiết kế viền và nền ảnh.

### 🟡 Mức độ: TRUNG BÌNH (Medium Priority)
- [ ] **Khắc phục lỗi ép kiểu Implicit Unknown (TS2322) trong các bảng quản trị:**
  - *Vị trí:*
    - `my-web/frontend/src/components/features/admin/AdminMerchantApprovalTable.tsx` (dòng 191, 205)
    - `my-web/frontend/src/components/features/admin/AdminMerchantFoodTable.tsx` (dòng 634, 635)
    - `my-web/frontend/src/components/features/admin/AdminSystemFoodTable.tsx` (dòng 254, 255)
    - `my-web/frontend/src/components/features/admin/AdminUserTable.tsx` (dòng 172)
  - *Vấn đề:* Các thuộc tính động (`avatar`, `legalDocs`, `description`, `image`) được khai báo dạng `unknown` trong `AdminTableItem` khi gán trực tiếp vào `MiniCardForAdmin` gây lỗi biên dịch.
  - *Giải pháp:* Thực hiện ép kiểu tường minh (`as string`, `as string | undefined`) hoặc kiểm tra kiểu dữ liệu an toàn (`!!item.legalDocs`).
- [ ] **Sửa lỗi hẹp kiểu mảng tĩnh của thuộc tính `metaBadges`:**
  - *Vị trí:* `my-web/frontend/src/components/features/admin/AdminMerchantFoodTable.tsx` (dòng 616-629)
  - *Vấn đề:* Suy luận kiểu tự động của mảng `metaBadges` giới hạn tập hợp thuộc tính `type` trong `("success" | "primary" | "danger")[]`, gây lỗi loại trừ khi chèn nhãn nháp có màu `'purple'`.
  - *Giải pháp:* Định nghĩa kiểu mảng tường minh khớp hoàn toàn với cấu hình Component nhận: `metaBadges: Array<{ label: string | React.ReactNode; type?: 'primary' | 'secondary' | 'success' | 'danger' | 'neutral' | 'purple' }>`.

### 🟢 Mức độ: THẤP (Low Priority)
- [ ] **Chuẩn hóa Localization (Loại bỏ Hardcode chuỗi tiếng Việt):**
  - *Vị trí:*
    - `AdminMerchantApprovalTable.tsx` (các chuỗi `"Tài liệu pháp lý:"`, `"Đăng ký:"`)
    - `AdminUserTable.tsx` (chuỗi `"Tham gia:"`)
    - Các nút điều khiển chế độ xem (View Mode): `"Bảng"`, `"Dòng (1x1)"`, `"Khối (2x2)"`
  - *Giải pháp:* Chuyển dịch toàn bộ các chuỗi giao diện thô tiếng Việt vào hệ thống hằng số `LABELS.ADMIN` trong file `labels.ts` tập trung.

---

## 📌 PHẦN 2: TÁI CẤU TRÚC "SETTINGSSECTION.TSX" (VI PHẠM KIẾN TRÚC)

Khắc phục các lỗi vi phạm nghiêm trọng về kiến trúc sạch (Clean Architecture) và nguyên lý Đơn trách nhiệm (Single Responsibility Principle):

### 🔴 Mức độ: CAO (High Priority)
- [ ] **Tách lớp xử lý logic thành Custom Hook `useSettings.ts`:**
  - *Vị trí:* `my-web/frontend/src/components/features/SettingsSection.tsx`
  - *Nội dung:* Di chuyển toàn bộ 11 `useState` độc lập và các hàm điều hướng nghiệp vụ (`onDeleteSubmit`, `onPasswordSubmit`, `onVerifySubmit`) sang một custom hook mới là `useSettings.ts`. Loại bỏ liên kết trực tiếp giữa View và API Service.

### 🟡 Mức độ: TRUNG BÌNH (Medium Priority)
- [ ] **Phân tách các Sub-components độc lập theo chức năng:**
  - *Vấn đề:* File `SettingsSection.tsx` hiện đang ôm đồm 4 phân khu chức năng khác nhau.
  - *Giải pháp:* Chia nhỏ thành các component con độc lập:
    - `ProfileSettingsTab.tsx` (Cập nhật hồ sơ cá nhân)
    - `SecuritySettingsTab.tsx` (Thay đổi mật khẩu)
    - `VerificationSettingsTab.tsx` (Xác thực Email tài khoản)
    - `DangerZoneSection.tsx` ( Danger Zone xóa tài khoản vĩnh viễn)

### 🟢 Mức độ: THẤP (Low Priority)
- [ ] **Localization chuỗi text trong `SettingsSection`:**
  - *Giải pháp:* Di chuyển các văn bản giao diện thô tiếng Việt của mục Settings vào hằng số `LABELS` tập trung.

---

## 📌 PHẦN 3: CÁC CẢI TIẾN HỆ THỐNG KHÁC (SECURITY & CONFIG)

Các tác vụ dọn dẹp kỹ thuật phụ trợ ở cả Backend và Frontend để tối ưu hóa quy chuẩn codebase:

### 🟡 Mức độ: TRUNG BÌNH (Medium Priority)
- [ ] **Đổi tên tham số gây hiểu lầm trong Category Module:**
  - *Vị trí:*
    - `category.controller.ts` (Dòng 35, 44, 57 & 67)
    - `category-group.controller.ts` (Dòng 37, 45, 54 & 64)
  - *Vấn đề:* Decorator `@GetUser('id') restaurantId: number` thực chất lấy ra `userId` của tài khoản đăng nhập để sau đó Service mới đi tìm `restaurantId`. Đặt tên biến là `restaurantId` tại Controller gây sai lệch context.
  - *Giải pháp:* Đổi tên biến tham số tại các Controller thành `@GetUser('id') userId: number`.
- [ ] **Xây dựng DTO xác thực cho API Đổi Mật Khẩu:**
  - *Vị trí:* `auth.controller.ts`
  - *Vấn đề:* API `change-password` nhận trực tiếp đối tượng thô từ `@Body() body` mà không có lớp ràng buộc dữ liệu đầu vào.
  - *Giải pháp:* Xây dựng lớp `ChangePasswordDto` tích hợp định dạng thư viện `class-validator` để kiểm soát độ dài và độ phức tạp mật khẩu mới.
- [ ] **Bật lại TypeScript Compile Strict ở Frontend:**
  - *Vị trí:* `my-web/frontend/next.config.ts`
  - *Giải pháp:* Gỡ bỏ thuộc tính bỏ qua lỗi TypeScript (`ignoreBuildErrors: true`) để đảm bảo các lỗi kiểu dữ liệu bắt buộc được giải quyết trước khi đóng gói production.

### 🟢 Mức độ: THẤP (Low Priority)
- [ ] **Nâng cấp công cụ Logger ở Backend:**
  - *Giải pháp:* Thay thế toàn bộ các câu lệnh `console.log` và `console.error` thô ở tầng backend bằng thư viện `Logger` chính thống của NestJS nhằm cải thiện khả năng ghi log tệp tin trong môi trường production.
